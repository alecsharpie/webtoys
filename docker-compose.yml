version: '3.8'

services:
  webtoys:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
      - webtoys_storage:/app/storage
    env_file:
      - ./backend/.env
    restart: unless-stopped

volumes:
  webtoys_storage: